#include <iostream>
#include <cmath>
using namespace std;
int main() {
    double x, a, n, j = 0;
    cin >> x >> a >> n;
    j = x;
    for (int i = 1; i <= n; i++) {
        j = pow((j + a), 2);
    }
    cout << j + a;
}
