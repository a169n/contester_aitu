#include <iostream>
using namespace std;
int main() {
  double a, n, x = 1, f = 0;
  cin >> a >> n;
  for (int i = 0; i <= n; i++) {
    x = x * (a + i);
    f = f + (1 / x);
  }
  cout << f;
  return 0;
}
