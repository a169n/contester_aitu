#include <iostream>
#include <cmath>
using namespace std;
int main() {
  double a, x = 1, n;
  cin >> a >> n;
  for (int i = 1; i <= n; i++) {
    x = x *  (a + i - 1);
  }
  cout << x;
  return 0;
}
