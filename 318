#include <iostream>
#include <cmath>
using namespace std;
int main() {
  double a, n, x = 1, f = 0;
  cin >> a >> n;
  for (int i = 0; i <= n; i++) {
    x =  1 / pow(a, 2 * i);
    f += x;
  }
  cout << (1 / a) + f - 1;
  return 0;
}
