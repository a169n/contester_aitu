#include <iostream>
#include <cmath>
using namespace std;
int main() {
  double x, b;
  cin >> x;
  b = x - pow(x, 3) / (6) + pow(x, 5) / (120) - pow(x, 7) / (5040) + pow(x, 9) / (362880) - pow(x, 11) / (39916800) + pow(x, 13) / (6227020800);
  
  cout << b;
}
